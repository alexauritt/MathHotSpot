- content_for :head do
  %title
    = "Lesson: #{@lesson.title}"
  = stylesheet_link_tag "global", "lessons", "math_display", "problem_type"

#lesson.lesson
  =render :partial => "lesson_header", :locals => { :lesson => @lesson }
  #lesson-problem-list
    -@problem_types.each do |problem_type|
      .math-display
        .problem-type-medium
          =render :partial => 'problem_types/problem_type', :locals => { :problem_type => problem_type }
  #lesson-links
    =link_to "Edit this Lesson", edit_lesson_path(@lesson)    
    |
    =link_to "Create new Problem Type", new_problem_type_path
    |
    =link_to "Find a Problem Type for this Lesson", new_problem_types_search_path
    |
    =link_to "Create a Worksheet for this Lesson", new_lesson_worksheet_builder_path(@lesson)
  






