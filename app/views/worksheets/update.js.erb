var newMathDisplay = "<%= escape_javascript(render(
  :partial => 'math_problems/problem_display.xhtml.erb',
  :locals => { :math_problem => @new_problem })) %>";

$('div#problem_<%= @problem_number %> div.math_display').replaceWith(newMathDisplay);

$.throbberHide();