#problem-type-search-categories
  .search-section-title ProblemTypes by Subject/Category
  - subjects.each do |subject|
    .subject
      = subject.title
      = render :partial => "categories/index_with_problem_types", :locals => {:subject => subject}
