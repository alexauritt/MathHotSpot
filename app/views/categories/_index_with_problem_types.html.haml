%ul.category_list
  - categories.each do |category|
    %li
      ="#{category.title}"
      %ul.problem_type_list
        - category.problem_types.each do |problem_type|
          %li
            =link_to problem_type.title, problem_type_path(problem_type)
            =problem_count_msg(problem_type)